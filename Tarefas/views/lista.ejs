<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: #343a40;
        }

        a {
            display: inline-block;
            margin-bottom: 20px;
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
            transition: color 0.3s;
        }

        a:hover {
            color: #0056b3;
        }

        form {
            background: #fff;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            width: 300px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        input[type="submit"] {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
            transition: background 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #218838;
        }

        ol {
            list-style: none;
            padding: 0;
            width: 300px;
        }

        li {
            background: white;
            padding: 10px;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        form[action*="finish"] {
            display: inline;
        }

        form[action*="finish"] input[type="submit"] {
            background-color: #ffc107;
            color: black;
        }

        form[action*="finish"] input[type="submit"]:hover {
            background-color: #e0a800;
        }
    </style>
</head>

<body>
    <h1>
        <%= lista.title %>
    </h1>
    <a href="/">Voltar para Listas</a>

    <form action=<%=`/lista/${lista.id}/task/create` %> method="post">
        <label for="title">Descrição da Tarefa</label>
        <input type="text" name="title" id="title">
        <input type="submit" value="Nova Tarefa">
    </form>

    <ol>
        <% lista.tasks.forEach(task=>{%>
            <li>
                <%= `${task.title} - ${task.done ? "Finalizada" : "Pendente" }` %>
                    <form action=<%=`/lista/${lista.id}/task/${task.id}/finish` %> method="post">
                        <input type="submit" value="Concluir">
                    </form>
            </li>

            <% }) %>
    </ol>
</body>

</html>